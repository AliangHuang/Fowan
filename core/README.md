# Fowan Core

This directory is reserved for the future cross-platform Rust core workspace.

The current Windows toolbox shell uses local mock data and does not depend on a
Rust engine yet. When the engine work starts, shared domain crates, protocol
types, storage abstractions, desktop engine code, and mobile-lite bindings should
live under this directory.
